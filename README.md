# Network Monitor (Raw Socket)

## Requisitos
- Linux/WSL com Python 3.10+
- Permissão CAP_NET_RAW (ou sudo)

## Instalação
```bash
python3 -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt
